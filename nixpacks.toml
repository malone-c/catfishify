providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "uv"]

[phases.install]
cmds = ["(cd backend && uv sync)"]

[start]
cmd = "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
